# AI Provider Configuration

- Configure provider keys in environment variables.
- Select provider/model per project via AI settings and bindings.
- Production note: use secret manager and key rotation policies.
